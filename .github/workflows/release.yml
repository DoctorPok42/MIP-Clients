name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build-ts:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./mip-client-ts
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Build package
        run: npm run build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ts-dist
          path: |
            mip-client-ts/dist/
            mip-client-ts/LICENSE
            mip-client-ts/README.md

  build-python:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./mip-client-python
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: Build package
        run: python -m build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: python-dist
          path: |
            mip-client-python/dist/
            mip-client-python/LICENSE
            mip-client-python/README.md

  release:
    needs: [build-ts, build-python]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download TypeScript artifacts
        uses: actions/download-artifact@v4
        with:
          name: ts-dist
          path: ts-dist

      - name: Download Python artifacts
        uses: actions/download-artifact@v4
        with:
          name: python-dist
          path: python-dist

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          body: |
            ## TypeScript Client
            ```bash
            npm install @mip-client/ts
            ```
            📦 [View on NPM](https://www.npmjs.com/package/@mip-client/ts)

            ## Python Client
            ```bash
            pip install mip-client-python
            ```
            📦 [View on PyPI](https://pypi.org/project/mip-client-python/)
          files: |
            ts-dist/mip-client-ts/dist/*.js
            ts-dist/mip-client-ts/dist/*.d.ts
            ts-dist/mip-client-ts/LICENSE
            ts-dist/mip-client-ts/README.md
            python-dist/mip-client-python/dist/*.whl
            python-dist/mip-client-python/dist/*.tar.gz
            python-dist/mip-client-python/LICENSE
            python-dist/mip-client-python/README.md

  publish-npm:
    needs: release
    runs-on: ubuntu-latest
    permissions:
      packages: write
    defaults:
      run:
        working-directory: ./mip-client-ts
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org/'

      - name: Install dependencies
        run: npm install

      - name: Build package
        run: npm run build

      - name: Publish to NPM
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --access public
        continue-on-error: true

  publish-pypi:
    needs: release
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    defaults:
      run:
        working-directory: ./mip-client-python
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: Build package
        run: python -m build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: mip-client-python/dist/
          skip-existing: true
